{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Clear Day Server",
            "type": "node",
            "request": "launch",
            "program": "${workspaceFolder}/server.js",
            "console": "integratedTerminal",
            "restart": true,
            "runtimeExecutable": "node",
            "env": {
                "NODE_ENV": "development"
            }
        }
    ]
}